# v0.10 Release Sign-off Record

Use this record in the release PR, issue, or tag notes before creating the
final v0.10 tag. Fill every required field or mark it as an accepted warning
with owner and rationale.

## Release Identity

- Release version:
- Release tag:
- Commit SHA:
- Sign-off owner:
- Sign-off date and timezone:
- Release PR or issue:

## Required Evidence

| Gate | Evidence Link or Command Output | Result | Owner |
| --- | --- | --- | --- |
| Tests | `go test ./...` |  |  |
| Vet | `go vet ./...` |  |  |
| Package lint | `make package-lint` |  |  |
| Local release check | `make release-check` |  |  |
| Installed-host security audit | `make security-audit` output when an installed host is available, or accepted warning for missing host evidence |  |  |
| Compatibility output | `arca show compatibility` from the release PR commit or local build |  |  |
| Upgrade preflight | `arca check upgrade` output when runnable, or accepted warning for missing daemon/package context |  |  |
| NETCONF client interop | `netconf-client-ncclient-evidence` and `netconf-client-libnetconf2-evidence` workflow artifacts, or verified local `artifacts/netconf-clients/` output |  |  |
| Standard NETCONF `:xpath` interop | `netconf-client-ncclient-standard-xpath-evidence` and `netconf-client-libnetconf2-standard-xpath-evidence`, or verified local `artifacts/netconf-clients/standard-xpath/` output |  |  |
| Applicable runbook sections | `docs/v0.10-operational-runbook.md` evidence |  |  |

## Compatibility Assertions

- [ ] `arca show compatibility` matches `docs/compatibility.md`.
- [ ] `internal/compat/policy.go` schema IDs match the release notes.
- [ ] Startup datastore is not advertised and `<startup/>` RPC requests are
      rejected as unsupported.
- [ ] NETCONF client interop artifacts show Arca XPath subset capability,
      default standard `:xpath`, no startup capability advertisement, and
      passing XPath RPCs.
- [ ] Standard XPath interop artifacts show standard `:xpath` advertisement
      with both ncclient and libnetconf2-family clients.
- [ ] Lab soak/restart/churn is not claimed as completed unless evidence is
      attached.
- [ ] RC package artifacts are not required for this v0.10 release decision;
      package builds may be verified separately when produced.

## Accepted v0.11 Deferred Gates

List every deferred gate accepted for this v0.10 release. Reference
`docs/v0.11-deferred-gates.md` for close criteria.

| Deferred Gate | Accepted for v0.10? | Rationale | Owner | v0.11 Tracking |
| --- | --- | --- | --- | --- |
| HA failover soak, FRR/VPP restart recovery, and 24-hour churn lab evidence |  |  |  |  |

NETCONF startup datastore is intentionally unsupported rather than accepted as a
v0.11 deferred gate. Reopen it only with an explicit future design record.

## Accepted Warnings

| Warning | Impact | Rationale | Owner | Follow-up |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## Final Decision

- [ ] Release is approved for v0.10.
- [ ] Release is blocked.

Decision notes:

```text

```
