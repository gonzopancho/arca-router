# v0.10 Release Sign-off Record

final v0.10 tag を作成する前に、この record を release PR、issue、または tag notes に記録する。必須 field はすべて埋める。埋められない field は、owner と rationale を付けて accepted warning として記録する。

## Release Identity

- Release version:
- Release tag:
- Commit SHA:
- Sign-off owner:
- Sign-off date and timezone:
- Release PR or issue:

## Required Evidence

| Gate | Evidence Link or Command Output | Result | Owner |
| --- | --- | --- | --- |
| Tests | `go test ./...` |  |  |
| Vet | `go vet ./...` |  |  |
| Package lint | `make package-lint` |  |  |
| Local release check | `make release-check` |  |  |
| Installed-host security audit | release-candidate host で実行した `make security-audit` output、または accepted deferred host evidence |  |  |
| Package metadata | DEB/RPM build metadata |  |  |
| Compatibility output | release-candidate package で実行した `arca show compatibility` |  |  |
| Upgrade preflight | release-candidate package で実行した `arca check upgrade` |  |  |
| NETCONF client interop | `netconf-client-ncclient-evidence` と `netconf-client-libnetconf2-evidence` workflow artifact、または verify 済み local `artifacts/netconf-clients/` output |  |  |
| Standard NETCONF `:xpath` interop | standard `:xpath` を enable または claim する場合のみ必須: `netconf-client-ncclient-standard-xpath-evidence` と `netconf-client-libnetconf2-standard-xpath-evidence`、または verify 済み local `artifacts/netconf-clients/standard-xpath/` output |  |  |
| Applicable runbook sections | `docs/v0.10-operational-runbook.ja.md` の evidence |  |  |

## Compatibility Assertions

- [ ] `arca show compatibility` が `docs/compatibility.md` と一致している。
- [ ] `internal/compat/policy.go` の schema ID が release notes と一致している。
- [ ] startup datastore は advertise されていない。
- [ ] NETCONF client interop artifact で、Arca XPath subset capability、
      default-mode で standard `:xpath` 非 advertise、startup capability 非 advertise が確認できる。
- [ ] standard `:xpath` を enable または claim する場合、standard XPath
      interop artifact で standard `:xpath` advertisement と XPath RPC pass
      が確認できる。
- [ ] lab soak/restart/churn は、evidence が添付されていない限り完了済みとして扱っていない。

## Accepted v0.11 Deferred Gates

この v0.10 release で accepted deferred gate とする項目をすべて記録する。close criteria は `docs/v0.11-deferred-gates.md` を参照する。

| Deferred Gate | Accepted for v0.10? | Rationale | Owner | v0.11 Tracking |
| --- | --- | --- | --- | --- |
| HA failover soak、FRR/VPP restart recovery、24-hour churn lab evidence |  |  |  |  |
| Formal NETCONF startup datastore capability |  |  |  |  |

## Accepted Warnings

| Warning | Impact | Rationale | Owner | Follow-up |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## Final Decision

- [ ] v0.10 release を approve する。
- [ ] release を block する。

Decision notes:

```text

```
